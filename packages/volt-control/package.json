{
  "name": "@volt/control",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "UI-agnostic core that drives the volt CLI/bridge (status/push/pull/build/health). Consumed by volt-vscode and volt-desktop.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./test-support": "./src/test-support.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test src",
    "test:e2e": "bun run --cwd ../volt-lsp-iec build && bun test test/e2e"
  },
  "dependencies": {
    "vscode-jsonrpc": "^9.0.2",
    "vscode-languageserver-protocol": "^3.18.3"
  },
  "devDependencies": {
    "@tsconfig/node22": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
