{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@opencode-ai/volt-cli",
  "version": "1.16.0",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/anomalyco/opencode"
  },
  "description": "Volt CLI — init, pull, push, status, build, merge for IEC 61131-3 projects via IDE bridges",
  "type": "module",
  "bin": {
    "volt": "./dist/bin.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsgo --noEmit",
    "test": "bun test",
    "record:language": "cd ../volt-lsp-st && tsc && cd ../volt-cli && tsc && node dist/tools/record-language.js",
    "conformance:report": "cd ../volt-lsp-st && tsc && cd ../volt-cli && tsc && node dist/tools/conformance-report.js"
  },
  "dependencies": {
    "@opencode-ai/volt-lsp": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@tsconfig/node22": "catalog:",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "typescript": "catalog:"
  }
}
