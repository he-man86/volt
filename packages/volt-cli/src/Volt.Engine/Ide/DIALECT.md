# CODESYS ⇄ TwinCAT: the measured vendor facts

**The one home for vendor facts.** It lives beside the contract they constrain (`Ide/`), not in a change folder,
because a change gets archived and these facts do not stop being true. When a measurement lands, correct it HERE
and delete the claim from wherever else it crept into a doc-comment.

Every row is cited from code by id (`DIALECT D4f`). `bun run check` fails if code cites an id that does not
exist here, or one marked `[RETRACTED -> X]` — both have happened, which is why the check exists.

> **This file was deleted once, by accident.** It lived under `PlcOpen/` and went out with that transport, while
> fourteen citations in production code kept pointing at it — including the justification for why TwinCAT
> re-finds a handle after every member create. The facts had outlived the layer they were filed under. It is
> filed under `Ide/` now because that is what they are about: how the two IDEs behave, not how one transport
> used them.

## The structural conclusion

> **The two vendors share an object model and differ only in ACCESS.** Measured 2026-08-29 by reflecting over
> both shipped `NWLObject.dll` assemblies: every interface Volt's graphical model uses is member-for-member
> identical. CODESYS hands over the live objects; TwinCAT hands over their serialization. Nothing in the engine
> branches on a vendor name, and nothing should start — what differs lives below the `Ide` seam, in the two
> drivers.

There is deliberately **no dialect abstraction**. The gap between the two vendors is not missing indirection, it
is missing EVIDENCE. Every entry cites code, a recorded fixture, or is marked **UNMEASURED** — `bun run check`
lists every marker on every run. Paths are relative to `packages/volt-cli/`.

## The object model (measured 2026-08-29, both assemblies reflected)

| id | fact |
|---|---|
| **N1** | **The NWL object model is IDENTICAL on both vendors, member for member.** TwinCAT ships `NWLObject.dll` 3.5.13.0 + `NWLObject.plugin.dll` (the concrete types) under `C:\TwinCAT.1\Components\Plc\`; CODESYS ships 4.1.0.0. Reflected: `INetwork` 27/27 members, `IOperand` 17/17, `IFlags` 18/18, `IBoxTree` 6/6, `IBoxTreeVisitor` 3/3, `IOutputItemList` 5/5, `IBoxTreeDemux` 5/5. 59 vs 61 types; the ENTIRE difference is `IPLCopenXMLAddDataProviderNWL` + `IPLCopenXMLAddDataReaderNWL`, CODESYS-only and dead with the PLCopen transport. A two-major-version gap and nothing Volt touches differs. So `CodesysNetworkReader`/`Writer` would port to TwinCAT unchanged — what is missing is ACCESS, not the model |
| **N2** | **The aspect's TYPE is the language on CODESYS — dispatch is a cast, not a lookup.** `STImplementationObject` / `NWLImplementationObject` / `CFCImplementationObject`. Nothing sniffs content and no language query is needed. Measured on one project: 271 ST + 1 CFC; on another 38 ST + 36 NWL |
| **N3** | **TwinCAT's `ImplementationText` IS the `<NWL><XmlArchive>` for a graphical body**, and `ImplementationXml` returns the SAME string — measured on a live POU, 7068 chars both. There is no second, richer representation over COM. `Language` is NOT on the object at all (RuntimeBinderException), so the body language is read from the archive's `DefaultViewMode` |
| **N4** | **The archive is a STRICT typed object graph with a per-type member contract.** The reader consumes members in order for the type it expects, so a member set that is close but not exact makes it mis-assign everything after the discrepancy. Measured against vendor-written `POU_PBD.TcPOU`: a `BoxTreeBox` carries `BoxType, Instance, OutputItems, Flags, InputFlags, InputItems, InputParam, OutputParam, CallType, EN, ENO, STSnippet, ContainsExtensibleInputs, ProvidesSTSnippet, Id`; a `BoxTreeOperand` carries `Operand, Id` and NO Flags; every item and operand carries `Id`. Writing an inferred subset produced twenty `.TcPOU` files TwinCAT could not open (`Value cannot be null. Parameter name: iILStatement`). **`TcNetworkWriter` therefore edits values in place and constructs nothing** |
| **N5** | **`CallType` is a SCALAR with a type attribute** — `<v n="CallType" t="Operator">And</v>` — not an object member. Reading it with an object accessor found nothing and classified every operator box as a plain function call |
| **N6** | **TcXaeShell is 32-bit and hosts the .NET FRAMEWORK CLR** (`clr.dll`, `Framework4.0.30319`). Any in-proc host must be net4x, x86/AnyCPU — a sibling of `Volt.Ide.Codesys` (net48), never the net8/x64 worker. **UNMEASURED:** whether the NWL objects are reachable from inside that process. `Process.Modules` across the WOW64 boundary cannot answer it — the same enumeration also fails to show `Microsoft.VisualStudio.Shell`, without which the shell cannot run — and enumerating `DTE.Commands` out-of-process hangs |
| **N7** | **`ITcPlcPou.DocumentXml` is a GET AND SET of the whole POU**, children included, in the vendor's own `TcPlcObject` format — 7742 chars for a POU whose body archive is 7068. Unused. It is the one-call write the per-child path lost, and unlike PLCopen it is not a translation. **UNMEASURED:** whether `set_DocumentXml` replaces children as well, and whether a vendor-written document round-trips through it byte-identically — which must be proven before it is used, per N4 |
| **N8** | **`ProduceXml` returns TwinCAT's `<TreeItem>` METADATA schema, not the file format.** An interface's own XML lists `ItemName, PathName, ItemType, ItemId, ChildCount, PlcItfDef, DeclarationExport, DocumentExport, VSProperties` — and carries no `<Property>` elements, so a property's accessors cannot be read from it. `ProduceXml()` with no argument omits children entirely; `ProduceXml(true)` includes them |

## Structure, placement and handles

| id | fact |
|---|---|
| **C2a** | **The export WORKS, and the E_FAIL was ours.** Measured: `PlcOpenExport` exports a root DUT (2012 chars, `<dataType>`), a root GVL (1983, `<globalVars>`) and a FOLDERED DUT (`VltProbeF.VltProbeDutF`). What fails is a BARE name for a foldered item — `PlcOpenExport('PLC_PRG')` answers *"Selection 'PLC_PRG' not found!"* — because the selection grammar is the DOTTED project-relative path, which `PouSelectionPath` already builds. The recorded E_FAIL came from `ExportPouXml` calling `EnclosingPou`, which climbs `node.Parent` until it finds a POU; for an item that has none the walk runs off the top of the tree, where `Parent` throws COMException E_FAIL. `PlcOpenExport` was never reached, so the vendor never refused anything. Fixed by choosing the export target by KIND (`IsInlinedInPou`), which cannot run off the tree |
| **C2b** | **AND THE SECOND REASON WAS ALSO OURS: `623` IS `TREEITEMTYPE_PLCDUTALIAS` ON TWINCAT, NOT A GENERIC DUT.** With the export fixed, DUT creates still failed — TwinCAT's importer validates against TC6 and rejected an empty `<baseType/>` as "incomplete content". True, and the object was malformed before any export: `CreateChild` refuses a null vInfo for 623 (*"Base class not specified!"*) and takes any string as the alias's BASE CLASS, so Volt — passing the body language — created every DUT as an alias to a type named **"ST"**. The per-child write only survived it because `WriteText` landed the real declaration afterwards; the document path exports first. Measured, creating with the real subtype: **606 → `<baseType><struct/>`, 605 → `<baseType><enum><values>`, 607 → union — all three round-trip their own export AND take a spliced declaration.** And one seed serves all four: create **606** and let the pushed declaration re-derive, exactly as CODESYS does with `DutType.Structure` — struct stays 606, an enum declaration becomes 605, a union 607, and an alias (`: INT;` or `: STRING(80);`) becomes 623 with the right base. `TcObjectModel.CreateChild`. **Every source kind now travels as ONE PLCopen document on both vendors; the `CanExportDocument` capability added for this is deleted** |
| **C2d** | **CODESYS's `create_pou` DOES take an implementation language.** Enumerated off the live scripting container: `create_pou(name: String, type: PouType [opt], language: Nullable\`1 [opt], return_type: String [opt], base_type: String [opt], interfaces: String [opt])`, and `create_function`/`create_function_block`/`create_program` each take one too. `CodesysObjectModel.CreateChild`'s "CODESYS's create_pou has no implementation-language parameter" was an inference. Volt still seeds and lets the import set the language, which works — but the reason recorded for it was false |
| **D4d** | **NEW — a TwinCAT PLCopen import INVALIDATES every handle to the item it replaced.** Any later COM call on that handle answers *"Item 'x' is deleted or invalidated by an ealier operation!"*. CODESYS's merge does NOT do this, and `PushService` carried the CODESYS reading as a general one ("`WriteXml` is a merge with no delete, so `pou` is not stale") — true there, false here. A graphical push reaches `RemoveOrphanChildren` immediately after `NetworkCode.Write` has imported, so on TwinCAT it was reconciling through a dead handle. **It went unseen for as long as it did because `BeckhoffDriver.ChildCount` caught the fault and answered `0`**: the orphan walk became a silent no-op, so a method deleted from a graphical POU survived in the IDE — the very bug the walk had been made unconditional to fix, still happening one layer down. Fixed by re-acquiring the item after the content write (`ItemLookup.Find`, hard-failing on a miss); the driver's accessor no longer fabricates `0`, so the next such fault is loud |
| **D4f** | **THE MOVE EXISTS. D4 and C5 were both wrong, and each for a reason worth keeping.** The dispatch surface of `ITcSmTreeItem` was ENUMERATED — `IDispatch::GetTypeInfo` → `ITypeInfo::GetFuncDesc`/`GetNames` on a live tree item — instead of probed by name. There is genuinely no `Move`/`Reparent` member; that half of D4 holds and is now settled rather than assumed. What the enumeration also gave was the real signatures, which two earlier probes had guessed at: `PlcOpenImport(bstrFile, options, bstrSelection, bFolderStructure)` — **four** arguments, of which Volt has ever passed two, and D4b's "a third argument naming a target folder is `DISP_E_TYPEMISMATCH`" was landing on `bstrSelection`, never reaching the folder flag at all. And `ExportChild(bstrName, bstrFile)` / `ImportChild(bstrFile, bstrBefore, bReconnect, bstrName)`. **The archive is a plain zip and the item's source path is an ENTRY NAME** — that is the "way to rewrite the path inside the archive" D4 said had not been found. Measured, all four cells: exporting `VltProbeF/VltProbePou` yields one entry `VltProbeF\VltProbePou.TcPOU` and importing it into `VltProbeG` gives `VltProbeG/VltProbeF/VltProbePou` (D4's path recreation, reproduced); **rename that entry to `VltProbePou.TcPOU` and the same import gives `VltProbeG/VltProbePou`** — a true move. It carries CHILDREN (an FB with a method arrives with `childCount=1`) and therefore graphical bodies, which `PushService`'s delete-and-recreate move can never do. Implemented as `TcItemArchive.Move`; only entry names are rewritten, every entry's bytes are copied through untouched |
| **D4j** | **AND IT IS RESTORABLE — the member's folder is an attribute in the POU's OWN file. This was the last thing blocking the single-document write on TwinCAT, and it is closed.** `ExportChild` REFUSES a member — *"The tree item 'Deep' cannot be exported seperately because it has no document file. Please export the parent node that contains the document!"* — which reads like the end of the road, and is instead the whole clue: a member is not a file because TwinCAT keeps the entire POU, members and all, in ONE `.TcPOU`. Read that file and the placement is right there. Measured live, the complete shape: `<Folder Name="Helpers" Id="{guid}">` elements that NEST by element, one path segment each; and on the member `FolderPath="Helpers\Inner\"` — full path, backslash-separated, TRAILING separator — with the SAME attribute on `<Method>`, `<Action>` and `<Property>` alike. So the relocation happens one level up: export the POU, rewrite the attribute (and create the `<Folder>` chain), delete, re-import. Verified on a textual AND an FBD member in one go: both land in the folder, bodies intact — the archive is the vendor's own format, so nothing is rebuilt from text. Cost, and it is real: placing a member is a full POU round trip, so a POU with N foldered members pays N of them, and each one INVALIDATES every handle into that POU. `PushService.RestoreChildFolders` therefore re-finds the POU once per member — it used to hoist that lookup out of the loop, which was correct only while CODESYS (whose move touches nothing but the moved object) was the sole driver that reached it |
| ~~D9~~ | **CLOSED — the grammar is the DOTTED project-relative path.** `POUs.PLC_PRG` and `VltProbeF.VltProbeDutF` export; a BARE name resolves only at the PLC-project root, and for a foldered item answers *"Selection 'x' not found!"*. That refusal, read as a vendor limit, is what produced the retracted C2 — see C2a |

## Creation and language

| id | fact |
|---|---|
| C6 | TwinCAT `CreateChild` cannot create `"LD"` — created as FBD (the ladder view rides along as `DefaultViewMode` in the NWL archive, which `TcPouReader` preserves on read-back). **The vendor limit is only that.** What actually failed every LD create on the single-document path was Volt's own guard: `CreateChild` seeds FBD, the document then shows an empty `<FBD/>`, and `PouSplice.SetBody` read that as "made graphical on purpose" and refused the very LD body the same push was creating. A body language guard protects an ENGINEER's diagram; on a CREATE there is not one yet, and the document cannot tell the two apart — hence `SetBody(… establishing:)` |
| **D19** | **TwinCAT `CreateChild` ACCEPTS a diagram language.** `CreateChild(name, 602, "", "CFC")` and `…, "SFC")` both succeed and produce a real diagram POU — measured live. Contrast C6, where `"LD"` is refused and silently becomes FBD: the vendor limit is specific to LD, not general to "languages CreateChild dislikes". Volt never uses this — it creates no diagram, because a diagram has no text form to push — but it is what authored `VltFixtureCfc`/`VltFixtureSfc` in the TwinCAT e2e fixture, which is the only reason live CFC/SFC coverage exists at all. |
| **~~D20~~** | **CLOSED — a library FUNCTION with no return type is a real CODESYS OPERATOR, not an object-model failure, and is now SKIPPED instead of throwing.** Measured on a live SP21 compile context: `AppendErrorString` and `ConcatX` (analyzation 4.1.0.0) have `POUType = LanguageModel.Operator.Function`, `Flags = None`, two `VAR_IN_OUT` and no return at all — exactly 2 of 588 library signatures. IEC has no void FUNCTION, so there is no honest text for one. The throw was not a cheap failure: every signature renders in ONE pass during `fetch`, so one unrenderable operator aborted the whole thing — a project referencing that library fetched 0 items instead of 593. `Render` already returns null for kinds it does not materialize and `FetchService` tallies those as `lib-render-null`, so the skip is counted, not silent. **The obvious fix was measured and rejected:** both are `{attribute 'hide'}`, but 40 of 586 signatures carry that attribute — including `SysFileOpen`/`SysFileRead`, which real code calls directly — so skipping on hidden-ness would make the LSP report unknown-identifier on valid programs. Still never `?? "BOOL"`: the choice is skip-or-crash, never skip-or-guess. |
| **C2c** | **TwinCAT HAS a library-signature surface** — `_ITcPlcLibraryManager.ProduceAllLibrarySignatures()` returns 181,179 chars of structured signatures (FB/function names, comments, typed inputs/outputs) on the fixture, out-of-process, today. So "TwinCAT has no resolved-library-signature surface yet" and ARCHITECTURE's "TwinCAT (out-of-process) can't extract" are both wrong; Volt simply implements no extraction there. The per-library `ProduceLibrarySignatures(pLibRef: VT_PTR)` wants a raw pointer an RCW does not satisfy — the all-libraries call needs no argument |
| **D21** | **Writing an INTERFACE property accessor's declaration CRASHES TcXaeShell** — `0x800706BE` `RPC_S_CALL_FAILED`, on read as well as write. Measured twice, years apart in repo time: once when interface-member creation was first implemented (the fix was to create accessors as bodiless `"ST"` stubs and write nothing), and once when accessor declarations were moved to the declaration aspect — that attempt took a green 141/0 suite to 135/9 and was reverted. An interface accessor declares that a getter/setter EXISTS and carries nothing else, so there is nothing to write; both drivers refuse the write outright (`WriteAccessor`). Accessor declarations are blank in every fixture and every live project measured, which is why nothing fails today — but the refusal is what keeps it that way |
| **N9** | **The TwinCAT DRIVER compiles for net48 unchanged** — measured by building `Driver/*.cs` + `Ide/*.cs` against `net48` with no source edits. Nothing in it needs net8: it is `dynamic` + COM interop, both of which net4x has. Taken with N6 (TcXaeShell is 32-bit .NET Framework), this means an in-proc TwinCAT host is possible ON THE CODE SIDE — the driver is already portable, and what is missing is only a way to get loaded, plus proof that the NWL objects are reachable once there. Only `Program.cs` (the out-of-process worker's entry point) is net8-bound, and a host is a different concern from a driver. **The other half is now CLOSED, negatively — see N10:** the NWL objects are not reachable in the shell at all, so the driver's portability buys nothing and an in-proc TwinCAT host is not worth building |
| **N10** | **CLOSED, NEGATIVE — the NWL objects are NOT reachable in TcXaeShell, because TwinCAT ships the scripting CONTRACT without its IMPLEMENTATION.** This settles the half of N9 that was left open, and it settles it against an in-proc host rather than for one. TwinCAT's `Components\Plc\Common\ScriptEngine.dll` carries the full CODESYS scripting surface — `IScriptDriverProjects.PrimaryScriptProject`, `IScriptProject` 1–8, `IScriptObject`, `IScriptEngine` 1–5 with `Execute`/`CreateScriptExecutor`, and the `[InjectedInstance]`/`[InjectedType]` attributes that are the very mechanism by which CODESYS hands a script its `projects` object. It is a contract and nothing more: **187 interfaces against 26 concrete classes, every one of them an attribute, a delegate or an exception, and ZERO implementing `IScriptEngine`, `IScriptDriverProjects` or `IScriptProject`.** The control experiment is what makes this conclusive rather than suggestive — CODESYS carries the same `Common\ScriptEngine.dll` AND `PlugIns\dc937c18-e9f0-434d-85b7-1b8f499e378a\4.2.0.0\ScriptEngine.plugin.dll`, the implementation; searched across the whole TwinCAT tree, **no `ScriptEngine*.plugin.dll` exists at any version**. IronPython 2.7.7 IS shipped (`Plc\LacBinaries\GAC_MSIL\IronPython`) and proves nothing on its own — it is a shared dependency sitting in the same GAC layout as Newtonsoft and SQLite, with no engine above it to host. So there is no component in the process that constructs an `IScriptDriverProjects`, therefore no project object graph, therefore no live `NWLImplementationObject` — the CODESYS route (`projects` → `IScriptObject` → the `Implementation` aspect) has no first step here. Getting one would mean bootstrapping the 3S component framework itself inside the shell, which is a different and far larger thing than loading a driver. **`NWLObject.plugin.dll` being present (N1) does not rescue this**: having the concrete types is not having a way to obtain the instance that belongs to a POU. The measured consequence is that TwinCAT's access to NWL stays through the SERIALIZATION — the `<NWL><XmlArchive>` inside the `.TcPOU`, reached via `ImplementationText` — which is the access asymmetry N1 already recorded, now known to be permanent rather than provisional. **PARTLY SUPERSEDED BY N12: the conclusion is right for the SCRIPTING engine and wrong for the COMPONENT FRAMEWORK.** The framework itself bootstraps headlessly outside the IDE, and the NWL objects construct through it. What stays true is the practical outcome — TwinCAT's access to NWL remains through the serialization — but for a different and narrower reason than "there is no component framework". |
| **N11** | **The `.TcPOU` archive IS the NWL object graph serialized — same classes CODESYS holds live — but the shipped DLLs are a NEAR-schema, not a complete one, and that difference is the whole point.** Measured by reflecting over `NWLObject.dll` + `NWLObject.plugin.dll` with a `ReflectionOnlyAssemblyResolve` hook (without it both assemblies load PARTIALLY — 15 and 34 loader errors on an unresolved `Core 3.5.13.15` — and every concrete type silently goes missing, which is what made a first pass report zero matches). The archive is a generic object-graph dump: `t=`/`cet=` are CONCRETE CLASS NAMES and `n=` are MEMBER names. The class names resolve in TwinCAT's own assemblies — `NWLImplementationObject`, `Network`, `Operand`, `Flags`, `OutputItemList`, `ParamList`, `BoxTreeAssign`, `BoxTreeOperand`, `BoxTreeDemux`, `BoxTreeParallel`, `BoxTreeTerminator` — so N1's interface-level identity now holds at the SERIALIZATION level too: this is not a Beckhoff format that happens to resemble CODESYS, it is the 3S object model written out by the 3S serializer. Member names are the PRIVATE BACKING FIELDS with the hungarian prefix stripped, confirmed directly: `Flags`→`_iFlags`, `Operand`→`_stOperand`, `ILLines`→`_ilLines`, `NetworkItems`→`_networkItems`, `Connectors`→`_connectors`, `OutputItems`→`_outputItems`, `BranchCounter`→`_nBranchCounter`, `ValidIds`→`_bValidIds`. **What stops this being a spec is specific and worth naming: `BoxTreeBox` — the type the archive names most often (15 distinct members across the fixtures) — DOES NOT EXIST as a class in any shipped PLC assembly.** Every sibling does, and `IBoxTreeBox` through `IBoxTreeBox8` do, and the literal string is present in `NWLObject.dll`, `NWLObject.plugin.dll`, `NWLEditor.plugin.dll` and `PLCopenXML.plugin.dll` — the concrete class is simply not there to reflect over. Two further names (`BoxTreeAssign.OutputItems`, `NWLImplementationObject.NetworkListComment`) have no matching field either, so even the prefix rule is a convention rather than a mapping. **Consequence, and it is a decision not an observation:** the DLLs are excellent for CONFIRMING what a measured archive means, and cannot be used to SYNTHESIZE one — so TwinCAT graphical create stays behind the byte-identity gate driven by real fixtures, exactly as the in-place writer was. Nor is any of it documented: there is no `.xsd` for `TcPlcObject` anywhere in the TwinCAT tree, and Beckhoff's published surface is the Automation Interface plus PLCopen import/export — the `.TcPOU` is documented as a source-control artifact, never as an authoring format. **CORRECTION (N12): `BoxTreeBox` DOES exist** — `_3S.CoDeSys.NWLObject.BoxTreeBox`, `TypeGuid {ACFC6F68-8E3A-4af5-BF81-3DD512095A46}`, in `NWLObject.plugin.dll`, alongside `Network` and `NWLImplementationObject`. Its absence here was a measurement artifact of the very partial-load this row warns about: `ReflectionOnlyLoadFrom` + a resolve hook still drops types silently, and the reflection was believed anyway. Read with `MetadataLoadContext` (or a raw `MetadataReader`, which loads nothing at all) every sibling appears. The row's CONCLUSION survives intact for a different reason — see N12 — but its central piece of evidence was false. |
| **N12** | **THE NETWORK-MERGE PRIMITIVE EXISTS, THE FRAMEWORK BOOTSTRAPS, AND THE ROUTE IS STILL BLOCKED — at the archive DIALECT, one layer below where N10/N11 placed the wall.** Chased to answer one question: can TwinCAT merge two networks, so a network holding two independent rungs stops coming back split (D25)? (a) **The primitive is there.** `INetwork.AppendTree(IBoxTree)` / `InsertTree(int, IBoxTree)` / `RemoveNetworkItem(int)`, and `INWLImplementationObject.AppendNetwork` / `InsertNetwork` / `ReplaceNetwork` / `RemoveNetwork(int)`. Merging B into A is `foreach tree in B: A.AppendTree(tree)` then `impl.RemoveNetwork(indexOfB)` — vendor mutators only, nothing constructed by Volt. (b) **The concrete classes ship and are constructible** (corrects N11). (c) **The 3S component framework bootstraps HEADLESS, outside the IDE** (corrects N10): `_3S.CoDeSys.Core.Components.ComponentManager` is public with a public ctor and static setup — `SetRootFolder` / `SetRelativeCommonFolder` / `SetRelativePlugInsFolder` / `SetRelativeProfilesFolder` / `SetTwinCATAssemblyFolder` (Beckhoff's own hook for out-of-IDE hosting) — then `new ComponentManager()`, `InitializePlugInCache(null)`, `ActiveProfileName = "TwinCAT PLC Control"`, `CreateSystemInstances(null)`. After that `ComponentModel.Singleton` RESOLVES where it previously threw `NullReferenceException` from its own ctor, and `cm.CreateInstance(TypeGuid)` returns a live framework-registered `NWLImplementationObject`. The 3S serializer then runs: it wrote a real object out as XML. **N10 was measuring the scripting engine, whose implementation is genuinely absent, and generalised from it to the framework, which is present.** (d) **Where it actually stops: the archive DIALECT.** A `.TcPOU` body is `<o t="…">` / `<v n="…">` / `<l2 n="…">`, and NOTHING in the shipped public surface reads or writes that. Measured as a full matrix — 4 readers (`_3S.CoDeSys.XmlArchive.XmlArchiveReader` + `CompactXmlArchiveReader`, `Beckhoff.TwinCAT.Storage.XmlArchive.XmlArchiveReader` + `CompactXmlArchiveReader`) x 4 document levels (`<NWL>`, `<XmlArchive>`, `<Data>`, the inner `<o>`): every 3S reader answers `XmlSchemaException`, every Beckhoff reader answers NULL and fills 0 networks. The 3S writers emit the VERBOSE form (`<Single Type="{guid}" Method="IArchivable">` / `<List2>`), compact included; the Beckhoff writer cannot even name its root (`ArgumentException: '' is not a valid local name`). The o/v/l2 dialect appears to belong to `Beckhoff.TwinCAT.Storage.TcPlcObjectArchive.WriteObjectDirectly`, whose signature takes an `ITcProjectSourceNode` — a live IDE object. **So the merge is reachable only IN-PROCESS, inside TcXaeShell, where the IDE owns both the object graph and its dialect.** That is the in-proc host N9 showed is possible on the code side and which does not exist for TwinCAT; building one (a VSIX/add-in in a 32-bit .NET Framework shell, N6) is a large piece of work whose only current payoff is D25's grouping fidelity. **Decision: not built. The gap stays documented at the assertion that finds it** (`test/e2e/graphical/roundtrip.test.ts`, the `multicoil` case) rather than papered over — and this row is what a future in-proc host starts from, because the hard half (framework bring-up) is now measured and works. |
| **D22** | **`PlcOpenImport` WORKS and is the only way to give TwinCAT a graphical body it does not have — but three of its documented properties are wrong on this install, each measured live.** (a) **`options=2` does not REPLACE.** Beckhoff document the enum as None(0)/Rename(1)/Replace(2)/Skip(3); importing over an existing POU with 2 left the original untouched and deposited the real body beside it as `VltE2E_fanout_1` — it RENAMED. Volt therefore DELETES the shell first and imports with 0, which makes the outcome independent of what the option means; safe because that path only runs for a body with no items, and the result is verified afterwards because an importer that does not understand a document produces an EMPTY POU rather than throwing. (b) **The DECLARATION does not travel.** A 3S `declaration` `addData` block was ignored outright — the POU arrived holding `PROGRAM <name>` and nothing else. The vendor's own export is the tell and agrees: it writes `<interface />`, EMPTY, for a POU that has declared variables. The declaration goes through `DeclarationText` afterwards. (c) **What DOES travel is the topology, and it is resolved properly.** From a document stating only boxes, operands and wires, TwinCAT built a complete archive: `BoxTreeAssign` with `OutputItems` `out1`/`out2` over a `BoxTreeBox` `AND` with `InputItems` `a`/`b`, `CallType Operator=And`, and every `Id` minted by the IDE — i.e. exactly the members Volt must never guess (C2b's lesson, and the reason `TcNetworkWriter` creates nothing). Fan-out survives as ONE assign with two targets. **Volt emits no resolved signature**: the vendor's export carries `inputparamtypes`/`outputparamtypes` `addData` holding the COMPILER's answer (an `AND` whose `OutputItems` is empty still reports `OutputParam.Types = ["BOOL"]`), Volt models none, and the importer re-derives them. |
| **D23** | **A freshly created graphical POU is FULLY PRESENT AND ENTIRELY BLANK, so "has no body" is not a blankness test.** `CreateChild` with a graphical language mints a complete archive — `NWLImplementationObject`, a `TypeList`, and one `Network` whose `NetworkItems` is empty (`EmptyGraphicalShell.TcPOU`, saved from the live IDE). `string.IsNullOrWhiteSpace(implementation)` therefore never fires on a create, and the push fell through to the in-place writer, which refused with "network 1 changes from 0 to 1 item(s)". The rule that holds is `TcArchive.HasNoItems`. And explicitly NOT "the archive root is null": that is also true of a TEXTUAL body, so routing on it would silently convert live ST into a diagram instead of refusing. |
| **D24** | **A BOX'S OWN OUTPUT PINS CANNOT BE COMPARED AGAINST A PUSHED MODEL — network text has no syntax for them.** `Outputs` appears nowhere in `NetworkTextWriter` or `NetworkTextReader`, so a text-derived `Box.Outputs` is ALWAYS empty, and `TcNetworkWriter` was comparing the archive's count against information the model can never carry. It could only pass when the archive's box also had zero outputs — true of every hand-authored fixture, and FALSE of every box the IDE has resolved: TwinCAT gives a resolved `AND` one output item holding an empty operand. The consequence was that such a body could be pulled and never pushed back ("an item changes from 1 to 0 output(s)" on a push that changed nothing), which went unseen only because no fixture had one until an import produced the first. An ASSIGNMENT's targets are a different thing and ARE carried by the text (`out := x`), so those are still written. |
| **D25** | **A PLCopen import DECIDES ITS OWN NETWORK BOUNDARIES, so the archive's shape is the IDE's answer and not a copy of what was pushed.** PLCopen FBD has no network element — the body is a flat list of `inVariable`/`block`/`outVariable` wired by `refLocalId` — and the importer groups them by what is actually WIRED TOGETHER. Measured: one network holding two disconnected statements (`t1(IN := a, PT := pt); done := t1.Q;`) comes back as TWO networks. Volt's per-network `localId` spacing (`10^10 * (order+1)`) does keep separate networks separate, but it cannot hold disconnected trees together inside one. **Measured across four shapes and now GATED** (`test/e2e/graphical/grouping.test.ts`): one connected tree → 1; one wire feeding two coils → 1 (a shared `refLocalId` is still ONE component); two disconnected sinks → 2; an FB call plus a separate output read → 2. **The rule is therefore ONE NETWORK PER CONNECTED COMPONENT, and it is predictable from the model before the IDE is touched** — `TcNetworkWriter.Unhoist` already folds shared-wire trees into one item, so the component count is its result count. That is what makes a per-network SPLICE sound: a single-component network can be rebuilt by the IDE and swapped back in, leaving every other network byte-identical, while a multi-component one is refused because re-creating it would split it and renumber everything after it. **One trap:** the `localId` also encodes the network INDEX, so a network rebuilt while still carrying its real `Order` makes the importer emit the empty networks in front of it too (an `Order` of 1 came back as 2 networks). A rebuilt network is renumbered to 0 — its real position is the slot it is spliced into, which the rebuild has no need to know. The consequence is that the in-place archive writer, which requires the model and the archive to agree on shape, legitimately refuses after a create — so a create stamps values through it only when the shapes match, keeps the IDE's grouping when the model carries nothing beyond structure, and FAILS when the model carries detail the import cannot express. |
| **D26** | **COIL STORAGE LIVES ON THE ASSIGNMENT TARGET IN THE ARCHIVE AND ON THE VALUE IN NETWORK TEXT — and nothing translated between them.** The archive puts `Set` on the `BoxTreeAssign`'s OutputItems operand (measured on a real ladder: `Flags=Negation,Set`); the published text format spells it as a trailing modifier after the VALUE (`out := a SET;`), and `NetworkTextWriter` renders modifiers from the value, never from the target. So a SET coil pulled from TwinCAT rendered as a PLAIN one: invisible in the workspace, and silently downgraded on the next create. **It hid behind a green round-trip test**, because the writer did not write target flags either — the archive kept its SET, nothing appeared to change, and the loss was only in what the engineer could see. CODESYS's reader already puts storage on the value, so translating here is the two vendors agreeing, not a TwinCAT special case. The write back is BIT-PRECISE rather than a whole-flags assignment: a target may carry modifiers the text has no form for (a negated coil), and rewriting the field wholesale would erase them on a push that never mentioned them. `Reset` still refuses — the vendor's flag set has no such bit. **CORRECTED (2026-08-31): the sentence "CODESYS's reader already puts storage on the value" was FALSE** — that reader took flags off the `BoxTreeAssign` node and dropped the assignment TARGET's own flags, so a SET or negated coil pulled from CODESYS rendered as a PLAIN coil and was downgraded on the next push. The row asserted the fix existed, which is how the gap survived being written down. The translation now lives in ONE place both drivers call (`Volt.Engine/Format/Network/CoilStorage`), read and write. **Also measured on the same pass: `IOperand.Flags` has NO setter** — storage is written by MUTATING the existing `IFlags` (`Negation`/`Set`/`Jump`/`Return`/`Rtrig`/`Ftrig`, every one settable), never by assigning a new object. |
| **D27** | **The library-signature format, measured — and it closes C2c.** C2c established that `_ITcPlcLibraryManager.ProduceAllLibrarySignatures()` WORKS out-of-process while Volt implemented no extraction; what was still missing was the shape of what it returns, which no fixture recorded. Captured live with `--probe-libsig` (181,179 chars on the fixture, now excerpted verbatim as `fixtures/tc-pou/library-signatures.xml`): **it is XML, and it is NOT one document** — each referenced library is emitted as its own `<Library>` root and they are CONCATENATED, so three libraries come back as three roots and any parser rejects the payload until it is wrapped. Each root carries `LibraryName` / `Version` / `Distributor`, which are precisely the three parts `LibraryManifest.Resolution` formats — so the join from a signature to its `.library` ref falls out instead of being guessed. Inside, `<TypeSignature type="…">` takes exactly five values, and they are the SAME five strings `LibSignatureRenderer` already switches on (both are 3S vocabularies): **`FunctionBlock` (102), `Function` (114), `Type` (25), `VarGlobal` (18), `Interface` (1)**. A FUNCTION's RETURN arrives as an `<Output>` NAMED AFTER THE FUNCTION (`CONCAT` → `STRING(255)`), which is exactly the shape `LibSignatureRenderer.LiftReturn` already lifts. **The surface is SHALLOWER than CODESYS's in one way that matters: `Type` and `Interface` are described by NAME ONLY** — no fields, no methods, on every one of the 26 — so Volt does not emit them. Rendering a member-less `Type` would produce `TYPE X : STRUCT END_STRUCT END_TYPE`, which does not omit the fields but ASSERTS THERE ARE NONE, turning every `x.field` an engineer writes into a false error; emitting nothing costs one unknown-type error at the declaration instead of a wrong one at every use. There are also no METHODS anywhere in this surface, so a library FB's methods are not resolvable on this vendor as they are on CODESYS. Measured end to end: 181,179 chars → 234 signatures → 230 shipped (26 name-only skipped by design, 4 return-less FUNCTIONs render-null — the same D20 class CODESYS hits). |
| **D28** | **THE I/O TREE: what a device REPORTS, and why its `ProduceXml` must never be the file.** Measured on a live XAE with real hardware configured (2026-08-30). (a) **Kind.** An EtherCAT master reports `ItemType 2`, an EK1100 coupler `ItemType 5` — both far outside the 601-657 PLC range `ItemKind.Map` covers, and colliding with nothing, so every device fell through as unmapped and was dropped by `fetch`, `refs` and `push` alike. `WalkIoDevices` had been paying a live COM round trip per device and emitting nothing an engineer ever saw. They are emitted as `ItemKind.PlcDevice` (695), the kind CODESYS already uses. (b) **`ProduceXml` on a device is its SETTINGS DUMP, not a descriptor** — 2.9KB for the master, 10.6KB for the coupler — carrying `AmsNetId` (which embeds the engineer's own machine address), `ObjectId`, `FieldbusAddress`, `PhysAddr`, `SerialNo`, DC timing windows and per-state transition commands. It must not become the file: this text IS the version-hash input, so it would publish a network address into git and rewrite the file on every unrelated fieldbus tweak. `TcDeviceDescriptor` renders the short identity block CODESYS renders instead (`Name`/`Type`/`ID`/`Version` from `ItemName`/`ItemSubTypeName`/`ItemSubType`/`ProductRevision`), through the shared `Descriptor(14)`. **`Vendor` is left EMPTY deliberately:** TwinCAT exposes a numeric `VendorId` (Beckhoff = 2) where CODESYS exposes a company NAME, and a different KIND of value under a shared label misleads rather than under-informs. (c) **Fields sit at different depths and repeat**: `ItemName`/`ItemSubType`/`ItemSubTypeName` are direct children of `TreeItem` but a slave's `ProductRevision` is at `EtherCAT/Slave/Info/`, while `Transition` and `Cmd` repeat several times — so lookup takes the element only when EXACTLY ONE carries the name, and drops the line otherwise rather than guessing. (d) **A device with children becomes a FOLDER holding its own descriptor**, a childless leaf a plain file beside its siblings — CODESYS's rule, applied here so a workspace laid out from either vendor has the same shape. |
| **D29** | **THREE SMALL COM FACTS, each measured because a fallback had been standing in for one.** (2026-08-30, live TcXaeShell 15.0.) (a) **`LookupTreeItem` answers `COMException HRESULT 0x98510001` ("Item 'x' not found") for a path that does not exist** — measured for a bogus path under an existing root and at the root alike. `LookupPath` had been catching EVERY `COMException`, so an RPC drop mid-push read as "absent" and was reported as "the PLCopen import produced no POU", while the HRESULT that `ShouldMarkDegraded` classifies was destroyed — the session never degraded and `Recover()` never ran. (b) **`SolutionBuild.Build(true)` IS synchronous**: a Clean+Build cycle returned after 758ms and 1230ms with `BuildState` already `vsBuildStateDone`(3) on return, never once observed as `InProgress`(2). The poll that followed it was therefore dead code, and its silent 10s expiry — falling through to report the PREVIOUS build's `LastBuildInfo` on a wire-visible boolean — was the bug. **Note the enum is 1-BASED** (`NotStarted=1, InProgress=2, Done=3`); reading it as 0-based inverts the test. (c) **An EMPTY Output pane throws `COMException 0x80004005` (E_FAIL) from `TextDocument`** — that is its ordinary state, not a fault. Of seven panes only `Build`, `TwinCAT` and `Source Control - Git` return text. Letting that reach the outer catch aborted the WHOLE diagnostic parse, so every build reported one synthetic "could not finish reading the build output" error and nothing else — reachable because the old name filter matched `Build Order`, which is empty. Panes are no longer identified at all: the diagnostic regex is itself the test, which needs no pane GUID (TwinCAT's own is undocumented and version-fragile) and no localizable caption. |
| **C7** | **AN UNWIRED `En` PIN READS AS `System.Boolean false`, NOT NULL — and it cost a whole POU.** Probed on a user's project: a plain `AND` box in a network an ENGINEER drew reports `En = False`. `CodesysNetworkReader` treated any non-null `En` as a WIRED enable and handed it to `ReadNode`, which dispatches on the runtime type name, found `Boolean`, and threw *"the graphical item 'Boolean' has no network-text form yet"*. The damage ran far past a missing enable: that throw makes the BODY unreadable, an unreadable body makes the ITEM unreadable, and `FetchService` then SKIPS it — so the POU was absent from the workspace entirely, with no error at the wire and no file in git. **It reached a user before a test because every graphical fixture Volt owns is one Volt itself created, and those carry a null `En`; only a body drawn in the IDE has the boolean.** The guard is applied to every node-valued member (`RValue`, `Input`, `En`, `GetTree`), not just `En`: they are read the same way and nothing says the vendor is consistent about which of them answers `false`. **It now HAS an offline gate**, which it did not when this row was written. `NwlInterop` reaches every vendor member by reflection and the reader dispatches on `GetType().Name`, so a plain C# class NAMED `BoxTreeBox` with `En = false` reproduces the shape exactly — `Volt.Ide.Codesys.Tests` (net48, like the in-proc host) does it in milliseconds with no IDE, and reverting the guard reproduces the original message verbatim. |
| **C8** | **A NETWORK'S TITLE AND COMMENT CARRY THE NEWLINE THE ENGINEER TYPED.** Measured on the same project: `Title = "testlabel\r\n"`, `Comment = "this is a comment\r\n"`. Network text puts the title in a QUOTED STRING on the `NETWORK` header line, so an untrimmed title emitted a quote that SPANNED TWO LINES — text that does not parse, which means such a POU could be pulled and never pushed back — and the trailing newline in the comment emitted an extra empty `//` line. Both drivers now trim on READ and compare with trailing whitespace IGNORED on write, so the vendor keeps its own bytes and a push that changed nothing still writes nothing. Recorded as a C row because it is not vendor-specific: nothing makes TwinCAT immune to an engineer pressing Enter, and its reader/writer were given the same treatment. |
| **C9** | **FAN-OUT WIRE IDS ARE NETWORK-SCOPED, NOT BODY-UNIQUE — so there was never a collision to mint against.** (2026-08-31, live CODESYS SP21 3.5.21.40.) `CodesysNetworkWriter` used to map every model `Demux.VarId` onto a fresh id from the aspect's own `GetNextBranchCounter`, on the stated worry that Volt's numbering "can collide with ids already in the POU". Measured directly by pushing the SAME id `g0` as the wire in two different networks: both came back as `g0`, each pairing with its own references. The vendor keys a wire within its network; the body-wide counter is how it *allocates*, not a uniqueness it *requires*. **What the minting cost was diff noise, and only the change gate kept it from being worse:** since the network is cleared and rebuilt, every wire in it got a new number, so a one-word edit moved `LET g0` to `LET g2` while the untouched network beside it kept `g1` — the engineer sees wires they never renamed, renamed, in the commit holding the edit they did make. **TwinCAT never had it** (`TcNetworkWriter` writes `d.VarId` straight through), so this was also an accidental divergence, and closing it removed a mapping layer rather than adding one. Gated by `test/e2e/graphical/parity-fixes.test.ts` ("editing one operand leaves every wire name alone"), which asserts both networks keep their names across an edit. **The dead end worth recording:** the first fix tried to REUSE an id below a `BranchCounter` watermark. There is no such member — `INWLImplementationObject` exposes only `GetNextBranchCounter()`, and `BranchCounter` is a private backing field that appears solely in the archive (N11) — so `NwlInterop.Int` answered 0 for the missing member and the whole branch was dead code that measured green against a hunch. |
| **C10** | **A REAL PROJECT'S LADDERS ARE NOT THE LADDERS VOLT WRITES, and 40% of them could be pulled and never pushed back.** (2026-08-31, live CODESYS SP21 3.5.21.40, `Lenze_MID-S100_V5_00_602_T51` — 411 items, 40 graphical POUs, **373 networks**, 90 of them LD.) Pulled through the bridge and fed straight back into `NetworkTextGate`, **152 of the 373 were REFUSED by Volt's own reader** — text the writer had just produced and could not read back. None of it was reachable from a fixture, because Volt only ever wrote the shapes it already knew how to read; every graphical fixture it owns is one it authored itself. Seven root causes, all in the shared format (so both vendors were affected equally): **(a) an unconnected pin had no text form** — it rendered as nothing, giving `( * iRPM * 6)`, `RESET := , PV := )` and `MOVE(, iDec)`, 110 networks. The fix is a POSITION, not a token: a sigil was tried (`?`) and withdrawn, because **CODESYS writes `???` into a box whose instance is unresolved** — a real compile error the engineer must see, five of them here, one as an assignment target — so `?` was already content and Volt carries `???` through verbatim. **(b) a positional call could not stand as a statement** (34): `MOVE(g0, iDec);`, a box with EN wired and its output connected to nothing, refused as "an authoring mistake rather than a shape the IDE gave us". **(c) `LET i<n>` was parsed instead of kept as text** (23), turning one `inVariable` into a whole call box that the next push would have BUILT. **(d) a single-consumer fan-out was dissolved** by a use-count heuristic (3), deleting a `BoxTreeDemux` the IDE holds — invisible in the text, which is why only an in-IDE check finds it. **(e) `NOT(x)` read as the negation modifier** (3), replacing a NOT box with a dot on a pin. **(f) a `"` in a network title was written raw** (2), truncating the title at the engineer's own quote. **(g) a dotted name an engineer spaced out (`a .b`) and a comment they indented** both came back rewritten. **The prefixes are load-bearing and were not being honoured:** `g<n>` is a wire, `i<n>` an opaque leaf, `en<n>` an enable echo — use count only breaks ties for names the writer did not mint. **Result: 373/373 clean, and 40/40 graphical POUs a live fixed point** (pushed back, re-pulled, byte-identical, nothing refused). **One asymmetry is deliberate:** an empty FIRST operand is accepted (14 measured) and `(a AND )` is still refused (0 measured) — inferring the second from the first is exactly the reasoning this file exists to prevent. Gated by `NetworkTextRoundTripTests.A_real_projects_shapes_round_trip_byte_for_byte` (15 cases, 13 red before the fix) and `test/e2e/graphical/real-project-shapes.test.ts`. |
| **D30** | **PLCOPEN CANNOT SPELL A BRANCH POINT THAT FEEDS ONE PLACE, and TwinCAT was silently collapsing it.** (2026-08-31, live TcXaeShell 15.0.) A wire is spelled in PLCopen by consumers SHARING a `refLocalId` — there is no element for the branch point itself — so with TWO consumers the importer rebuilds the `BoxTreeDemux` (D22c), and with ONE there is nothing to tell it apart from an ordinary direct connection. Measured by pushing `LET g0 := (a AND b); out := g0;`: it came back `out := (a AND b);`, the branch point gone from the drawing, **and the push ACCEPTED**. Found only because the same body was pushed to BOTH bridges — CODESYS builds it natively, so a CODESYS-only test proves nothing here, and the text is identical either way so no offline test can see it at all. `TcPlcOpenWriter.EmitDemux` now counts consumers before emitting and REFUSES a single-consumer wire, with the same "create it in the IDE and pull it" answer the terminator and parallel arms give. **The boundary is exact and the cost is small:** this is the CREATE path only (a PLCopen import is TwinCAT's sole route to a body it does not have), while editing an existing body goes through `TcNetworkWriter`, which writes `VarId` straight into the archive and keeps single-consumer wires perfectly well — so a pulled TwinCAT body still round-trips, and only CREATING one from text carrying a branch point is refused. **Two more forced differences share this test file** and are refusals rather than reshapes: an unconnected pin (a `Terminator`, C10a) and a parallel branch have no PLCopen form either. Gated by `test/e2e/graphical/real-project-shapes.test.ts`, which asserts the INVARIANT rather than the outcome — *exact round-trip, or an explicit refusal, never accepted-and-reshaped* — because that is the difference between costing an engineer a detour through the IDE and costing them a drawing they will not know changed. |
| **C11** | **AN UNCONDITIONAL `JMP`/`RETURN` BUILT AN ITEM HOLDING NOTHING, and neither IDE would save it.** (2026-08-31, live CODESYS SP21 + TcXaeShell 15.0.) `NetworkTextReader` built a bare `RETURN;` as `Assign(value: null, targets: [])` and a bare `JMP name;` as `Assign(value: null, targets: [name])` — an item with no input and, for the return, no output either. CODESYS answered *"Object reference not set to an instance of an object"* on save and TwinCAT *"Value cannot be null. Parameter name: source"*; neither error named the cause, and the CONDITIONAL form worked on both vendors all along, which is the tell — the only difference is the missing value. **The fix reuses a measured vendor shape rather than inventing one:** `BoxTreeTerminator` with no input is how the archive spells a rung end that nothing drives (it is what `coil := ;` reads back as, D26), so that is what an unconditional jump or return now carries. `Goto` treats it as unconditional on the way out, so the text is unchanged. **CODESYS now round-trips `RETURN;`, `JMP name;` and both conditional forms byte-identically.** Gated by `test/e2e/graphical/labels.test.ts`. |
| **C12** | **VENDOR SPLIT POINTS DO NOT OCCUR: zero across 729 networks in two real projects.** `CodesysNetworkReader` refuses a network carrying `GetSplitPoint(0)` because network text has no form for it, and that refusal has now been exercised against a second independent project — `Lenze_MID-S100` (411 items, 373 networks) fetched with **zero reader refusals**, on top of the 356 networks of the first survey. The list is not fan-out (that is `Demux`, 573 occurrences in one project alone). **Nothing to build:** the refusal stands, it is honest, and no real body has ever tripped it. Worth re-checking only if a project ever fails to materialize an item with that message. |
| **D31** | **PLCOPEN `<return>` WAS REFUSED WITHOUT EVER BEING TRIED, and a return's targets were compared as outputs.** (2026-08-31, live TcXaeShell 15.0.) Three findings, one shape. **(a) The refusal was assumed.** `TcPlcOpenWriter` answered *"contains a return, which Volt cannot express as PLCopen"* — but `<return>` is a TC6 element sitting beside `<jump>` in the same content model, and Volt was already emitting jumps successfully. `EmitReturn` is `EmitJump` minus the label attribute; a conditional return now imports and round-trips. **(b) An UNCONDITIONAL jump or return really is rejected**, and this one is measured by single-variable comparison: the identical document WITH a `connectionPointIn` imports and round-trips, WITHOUT one the importer rejects the whole scratch object with `Value cannot be null. Parameter name: source`. The importer wants control flow WIRED. Volt now refuses that legibly instead of letting a vendor null-reference reach the engineer. **(c) `WriteOutputs` compared a control-flow item's `Targets` against the archive's `OutputItems`** — but a jump's single target is its LABEL and a return has none, while TwinCAT's imported return item carries one output slot holding an empty operand. So a body TwinCAT had just built from Volt's own import could be pulled and never pushed back (*"an item changes from 1 to 0 output(s)"*). **This is the third instance of one mistake** — comparing the archive against information a text-derived model cannot carry — after `Box.Outputs` and the FB `BoxType`, and it is worth reading as a pattern rather than three bugs. Gated by `test/e2e/graphical/labels.test.ts` (5 cases, both vendors). |
| **C13** | **A JUMP RIDES ON THE TARGET OPERAND, NOT ON THE ITEM — every jump Volt wrote was a COMPILE ERROR, and a byte-identical round trip said it was fine.** (2026-08-31, live CODESYS SP21 3.5.21.40, found by a user opening a POU Volt had just created.) `CodesysNetworkWriter` set `Jump` on the `BoxTreeAssign` item via `Flagged(asg, a.Flags)`. The IDE does not read it there: it drew the rung as an ordinary coil assigning to the label, and the build answered `Identifier 'Done' not defined`, `'Done' is no valid assignment target`, and `The label 'DONE' has not been referenced` — the label existed and nothing jumped to it. **Moving the bit to the target operand — where this vendor already keeps coil storage (D26) — turned 2 errors and 1 warning into a clean build.** **CORRECTION, from the parity review that followed:** an intermediate step here concluded "the item bit was Volt's own invention" because stripping it still built clean. That was wrong, and the vendor's own output says so — a jump built by TwinCAT's PLCopen importer (`VltProbe_Jump.TcPOU`) carries `Flags = 4` on the output `Operand` AND on the `BoxTreeAssign`. Only the operand is load-bearing for the compiler, but "it compiles" is a weaker claim than "it is what the IDE would have written", and N1's member-for-member identity makes the other vendor's importer evidence about this one. Volt writes both. **RETURN stays on the ITEM**, and the asymmetry is the model's rather than a preference: a return has no target operand to carry anything, and a POU built with the bit there compiles clean. **The reader had the mirror-image bug and it was the worse half:** it read `Jump` from the item too, so it only ever recognised jumps VOLT had written — a jump an ENGINEER drew has the bit on the operand alone and came back as a plain coil assigning to the label, silently turning their control flow into an assignment. **Why no test caught it:** Volt wrote the bit to the item and read it back from the item, so `labels.test.ts` saw byte-identical text while the IDE disagreed with both halves — a self-consistent round trip is exactly the shape a test cannot see through. The jump and return tests now assert a BUILD, counting the project's errors and warnings before and after rather than filtering by POU name (a CODESYS build diagnostic carries no item attribution — `Identifier 'Done' not defined` names nothing — so `ensureCompiles`'s prefix filter would have found zero). **TwinCAT is unaffected**: its create path emits a real PLCopen `<jump>` and the IDE builds the item itself. |
| **D32** | **A MEMBER'S IMPLEMENTATION CANNOT BE SET THROUGH `ImplementationText`, AND THE ARCHIVE ROUTE THAT CAN IS BLOCKED BY HANDLE INVALIDATION.** (2026-08-31, live TcXaeShell 15.0.) `WriteText` assigns `ImplementationText`; on a POU already holding an NWL archive TwinCAT replaces the archive, but on a METHOD, an ACTION or a property ACCESSOR the same assignment stores the archive AS TEXT. The push reports success, the body round-trips (Volt reads its own archive straight back), and the project will not build — `Unexpected Token '<' found`, `';' expected instead of 'NWL'`, `((((((NWL > !!!'ERROR'!!!) < XmlArchive) …`. **Measured twice**, once by observing it and once by disabling the refusal that now stands there and watching it recur. **The route that CAN work is the archive** — export the POU, rewrite the member's `<Implementation>`, import it back — the same mechanism `MoveMember` already uses, and for the same reason (a member is not a file; TwinCAT stores the whole POU in one `.TcPOU`). The rewrite itself is understood and was proven against a real vendor document: a member is `<Method Name=…>`/`<Action Name=…>`, an accessor is `<Get Name="Get">` nested in `<Property Name=…>`, each with its own `<Implementation><ST><![CDATA[…]]></ST>`; the `<ST>` is replaced by the `<NWL>` element, and the member's `<LineIds Name="POU.P_G.Get">` must be REMOVED — that is per-textual-body bookkeeping and an IDE-drawn ladder carries none. **WHAT BLOCKS IT IS THAT `ExportChild` DOES NOT SEE TREE-NODE WRITES**, and that is measured directly rather than inferred. Dumping both states at the moment of export: the live node read back `out := a;` — the write had landed — while the archive `ExportChild` produced for the same POU in the same instant carried `<Implementation><ST><![CDATA[]]></ST></Implementation>`, a skeleton declaration, and the member's declaration still at its create seed (`METHOD M_G : FBD`). `ImplementationText`/`DeclarationText` write the system-manager tree node; `ExportChild` serializes TwinCAT's own persisted document model, and `FlushPendingWrites` (`File.SaveAll`, a DTE shell command) does not bridge the two — calling it immediately before the export changed nothing. Two earlier readings were wrong and are recorded so they are not re-derived: it is NOT handle invalidation (the live read through that same handle was correct) and NOT a disk lag (after an ordinary push the `.TcPOU` on disk is correct). **SOLVED by inverting the order**, which is why the two states never have to agree: the way through is — resolve the bodies, do the export/rewrite/import FIRST so the archive is the only writer of member implementations, re-find the POU and its members afterwards (the import kills those handles, D4d), and only then write the declarations and the POU's own body through the tree, which the archive re-import leaves alone because `WriteText` skips a null implementation. Implemented in `BeckhoffDriver.WriteContent` (`Collect`/`Textual`/`WriteMemberBodies`); the rewrite itself is gated by `TcMemberBodyTests` against a real vendor document, and a graphical method, action and property accessor now round-trip and COMPILE on both vendors. Until then a graphical body on a member is REFUSED with a reason; editing one that already exists is unaffected, since that takes the in-place archive path. |
