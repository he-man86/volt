<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <IsPackable>false</IsPackable>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
    <PackageReference Include="xunit" Version="2.9.2" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\src\Volt.Cli.Connector.Core\Volt.Cli.Connector.Core.csproj" />
    <!-- Also the bridge Core, ONLY so WireContractTests can pin the connector's anonymous `connect` body against
         Volt.Engine.Wire.ConnectRequest — the one wire DTO still declared twice (the health row is not: it lives in
         Volt.Cli.Transport.Wire, which both sides reference). The production connector deliberately does NOT
         reference Core, and this reference exists for that single test; when ConnectRequest comes down, it goes. -->
    <ProjectReference Include="..\..\src\Volt.Engine\Volt.Engine.csproj" />
  </ItemGroup>
</Project>
