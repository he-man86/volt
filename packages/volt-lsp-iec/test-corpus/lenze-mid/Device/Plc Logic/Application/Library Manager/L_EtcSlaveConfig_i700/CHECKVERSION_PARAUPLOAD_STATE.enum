TYPE CHECKVERSION_PARAUPLOAD_STATE :
(
	CVUP_INITIALIZE,
	CVUP_CHECK_SLAVE_STATE,
	CVUP_GET_FILE_SIZE,
	CVUP_FILE_OPEN,
	CVUP_GET_BUFFER_INDEX,
	CVUP_GET_BUFFER_POINTER,
	CVUP_FILE_READ,
	CVUP_READ_CHECKSUM_FROM_SLAVE,
	CVUP_FILE_CLOSE,
	CVUP_ERROR,
	CVUP_DONE
);
END_TYPE