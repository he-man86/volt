{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@opencode-ai/volt-bridges",
  "version": "1.16.0",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/anomalyco/opencode"
  },
  "description": "Volt IDE bridges — C# binaries with shared Core (Beckhoff TwinCAT, CODESYS, more vendors coming)",
  "type": "module",
  "scripts": {
    "build": "dotnet build src/VoltBridge.Beckhoff/VoltBridge.Beckhoff.csproj -c Release --nologo",
    "build:all": "dotnet build src/VoltBridge.Beckhoff/VoltBridge.Beckhoff.csproj -c Release --nologo && dotnet build src/VoltBridge.Codesys/VoltBridge.Codesys.csproj -c Release --nologo",
    "build:package": "powershell -File build-bridges.ps1",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@tsconfig/node22": "catalog:",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
