# Volt — status & roadmap

Status and roadmap live in OpenSpec. Run `openspec list` for live, per-task status.

- **Shipped** (the current system) → the capability specs in `openspec/specs/`, captured by the
  archived `review-*` changes.
- **In-flight / planned** → `openspec/changes/`:
  `unify-ide-sync-ui` · `desktop-distribution` (B) · `commercial-landing` (W5) ·
  `deploy-revenue-cloud` (W6) · `connector-installer` · `docs-site`
- **Verify the fork still holds:** `bun volt-scripts/sync.ts`
