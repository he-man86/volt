# Moved

The CODESYS ⇄ TwinCAT dialect census now lives beside the code it describes:

**`packages/volt-cli/src/Volt.Engine/PlcOpen/DIALECT.md`**

It was written as this change's evidence base, but it is not evidence *for a change* — it is a standing
description of two vendors, and a change folder gets archived. Keeping it here would have made the archived copy
the one a future reader finds, and it would have gone stale the first time a §5 measurement landed.

`tasks.md` §5.1 is the task that moved it.
